apiVersion: v1
kind: Service
metadata:
  name: hospital-api-service
  namespace: hospital-api
  labels:
    app: hospital-api
spec:
  type: NodePort
  selector:
    app: hospital-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30000